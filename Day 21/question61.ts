// enum is a special type of list
// it has keys and values
// if you want to assign value, you can else it will be '0' by default.
enum vehicles {
    car,
    truck,
    motorcycle
}
console.log(vehicles.car)
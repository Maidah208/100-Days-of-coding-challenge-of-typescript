function caseConversion(str: string){
    let lowerCase = str.toLowerCase();
    let upperCase = str.toUpperCase();

    console.log(`Lower Case: ${lowerCase}\nUpper Case: ${upperCase}`)
}

caseConversion("heLLo WorLd!")

